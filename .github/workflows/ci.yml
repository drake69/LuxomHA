name: ESPHome CI

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  unit:
    name: Unit tests (host)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build & run pure-logic tests
        run: |
          c++ -std=c++17 -Wall tests/test_luxom_proto.cpp -o /tmp/luxom_tests
          /tmp/luxom_tests

  coverage:
    name: Coverage (pure logic)
    runs-on: ubuntu-latest
    needs: unit
    continue-on-error: true   # informational: never red the build on coverage/upload
    steps:
      - uses: actions/checkout@v4
      - name: Build with coverage & run
        run: |
          # two-step build so the .gcno/.gcda land next to a predictable object name
          g++ -std=c++17 -O0 --coverage -c tests/test_luxom_proto.cpp -o test_luxom_proto.o
          g++ --coverage test_luxom_proto.o -o luxom_cov
          ./luxom_cov
          gcov test_luxom_proto.o   # produces luxom_proto.h.gcov etc.
      - name: Upload to Codecov
        uses: codecov/codecov-action@v4
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          fail_ci_if_error: false
          verbose: true

  validate:
    name: Validate configs
    runs-on: ubuntu-latest
    needs: unit
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      - name: Provide CI secrets
        run: cp tests/ci_secrets.yaml secrets.yaml
      - name: esphome config
        run: |
          uv sync
          uv run esphome config luxom_gateway.yaml
          uv run esphome config luxom_cover_discovery.yaml

  compile:
    name: Compile firmware
    runs-on: ubuntu-latest
    needs: validate
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      - name: Cache PlatformIO / ESPHome build
        uses: actions/cache@v4
        with:
          path: |
            ~/.platformio
            ~/.cache/pip
            .esphome
          key: esphome-build-${{ hashFiles('pyproject.toml', 'uv.lock') }}
          restore-keys: esphome-build-
      - name: Provide CI secrets
        run: cp tests/ci_secrets.yaml secrets.yaml
      - name: esphome compile
        run: |
          uv sync
          uv run esphome compile luxom_gateway.yaml
          uv run esphome compile luxom_cover_discovery.yaml
